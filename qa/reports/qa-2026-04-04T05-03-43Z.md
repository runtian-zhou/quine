# QA Report — 042 Advanced Memory Scopes and Policy Controls

**Status**: passed

## Scope

Validated feature `042` on branch `feature-042-advanced-memory-scopes-and-policy-controls`.

## Focused Coverage

- `cargo test -q -p quine-core engine::tests::targeted_recall_prefers_agent_scope_when_configured_for_narrower_conflicts -- --exact`
- `cargo test -q -p quine-harness local::tests::advanced_memory_policy_denies_agent_writes_without_fallback -- --exact`
- `cargo test -q -p quine-core`
- `cargo test -q -p quine-harness`

## Workspace Gates

- `cargo build`
- `cargo test`
- `cargo clippy --all-targets -- -D warnings`
- `cargo fmt --all -- --check`

## Evidence

- Prompt-time recall now resolves deterministic readable scopes and prefers the configured conflict winner across overlapping project and agent memories.
- Extraction writes target exactly one resolved writable scope and do not silently fall back to project scope when the selected narrow scope is policy-constrained.
- Session context snapshots carry scoped durable-memory diagnostics through the existing memory inspection path.
