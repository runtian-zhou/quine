# QA Report — Feature 052 Persisted Permission Rules and Sources

**Status**: passed

## Scope

Verified that Quine now loads durable permission rules from trusted user and project config locations, applies deterministic source precedence during evaluation, preserves rule-source provenance in runtime diagnostics, and keeps session-only rules distinct from persisted rules.

## Automated Evidence

- `cargo test -p quine-harness config -- --nocapture`
  - passed
  - covers:
    - trusted parsing of user and project `permissions.yaml` documents
    - invalid-config rejection with actionable path details
- `cargo test -p quine-core permission:: -- --nocapture`
  - passed
  - covers:
    - source-precedence evaluation across session and persisted rules
    - winning-rule provenance on permission outcomes
    - explicit `ask` rule handling and non-interactive fallback behavior
- `cargo test -p quine-harness get_session_context -- --nocapture`
  - passed
  - covers:
    - persisted and session-only rule partitions remaining distinguishable in inspected runtime state
    - additive permission diagnostics compatibility through the existing session-context surface
- `cargo test`
  - passed

## Live Daemon Evidence

- Fixture setup:
  - user config at `/tmp/quine-052-qa.DoUcNk/home/.quine/permissions.yaml`
    - `allow` `read` for tool `read_file`
  - project config at `/tmp/quine-052-qa.DoUcNk/project/.quine/permissions.yaml`
    - `deny` `execute` for tool `bash`
- Daemon start:
  - `HOME=/tmp/quine-052-qa.DoUcNk/home /Users/runtianz/quine/target/debug/quine daemon start --socket /tmp/quine-feature-052.sock`
  - daemon working directory: `/tmp/quine-052-qa.DoUcNk/project`
- Direct JSON-RPC checks against the live daemon:
  - `create_session` returned session `1b0f6880-d914-4dea-a7d9-838512598fb4`
  - `get_session_context` on that session reported:
    - `permission_diagnostics.workspace_root = "/private/tmp/quine-052-qa.DoUcNk/project"`
    - `permission_diagnostics.rules.user[0]`
      - `effect = "allow"`
      - `request_scope = "read"`
      - `target.kind = "tool"`
      - `target.name = "read_file"`
      - `source_path = "/tmp/quine-052-qa.DoUcNk/home/.quine/permissions.yaml"`
    - `permission_diagnostics.rules.workspace[0]`
      - `effect = "deny"`
      - `request_scope = "execute"`
      - `target.kind = "tool"`
      - `target.name = "bash"`
      - `source_path = "/private/tmp/quine-052-qa.DoUcNk/project/.quine/permissions.yaml"`
    - `permission_diagnostics.rules.session = []`
- This confirms the live daemon bootstraps trusted persisted permission rules from both supported sources and exposes the preserved provenance through the existing context-inspection path.

## Notes

- The full workspace test run initially exposed a parallel-test env leak because the new config tests mutated `HOME` process-wide. The final merged tree restores those env vars under a test lock, and the clean rerun of `cargo test` passed.

## Full Workspace Gates

- `cargo build`
  - passed
- `cargo test`
  - passed
- `cargo clippy --all-targets -- -D warnings`
  - passed
- `cargo fmt --all -- --check`
  - passed
