# QA Report — Feature 047 Filesystem Permission Boundaries

**Status**: passed

## Scope

- `features/047-filesystem-permission-boundaries.md`
- `crates/quine-core/src/permission/path.rs`
- `crates/quine-core/src/permission/context.rs`
- `crates/quine-core/src/permission/request.rs`
- `crates/quine-core/src/permission/engine.rs`
- `crates/quine-core/src/engine.rs`

## Targeted Coverage

- `cargo test -p quine-core permission::path::tests::workspace_root_allows_resolved_in_bounds_paths -- --exact --nocapture`
- `cargo test -p quine-core permission::path::tests::additional_root_allows_resolved_in_bounds_paths -- --exact --nocapture`
- `cargo test -p quine-core permission::path::tests::outside_all_roots_is_denied -- --exact --nocapture`
- `cargo test -p quine-core permission::path::tests::traversal_is_evaluated_on_final_resolved_target -- --exact --nocapture`
- `cargo test -p quine-core permission::path::tests::symlink_escape_is_denied_if_supported -- --exact --nocapture`
- `cargo test -p quine-core --test filesystem_permission_boundaries -- --nocapture`
- `cargo test -p quine-core engine::tests::execute_tool_call_denies_outside_workspace_write_boundary -- --nocapture`

## Expected Runtime Checks

- Shared permission evaluation denies path targets outside approved roots.
- Resolved in-bounds paths remain allowed.
- An outside-root `apply_patch` attempt fails as a permission denial and does
  not create the file.

## Results

- Focused coverage passed for all exact selectors above.
- The integration suite in `crates/quine-core/tests/filesystem_permission_boundaries.rs` passed and exercised `read_file`, `find`, and `apply_patch` through the public core loop.
- Full workspace validation passed:
  - `cargo build`
  - `cargo test`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo fmt --all -- --check`

## Daemon Evidence

- Started the daemon with `source .env && cargo run --bin quine -- daemon start --socket /tmp/quine-047.sock`.
- Outside-root denial scenario:
  - `cargo run --bin quine -- run --json --socket /tmp/quine-047.sock "Use apply_patch to create ../qa-outside-root-denied.txt containing exactly OUTSIDE_ROOT_DENIED_047."`
  - Returned session id `f69a27c9-f8b5-4656-849a-cdbd7801a239`.
  - JSON response reported `permission denied: Write access denied outside approved roots`.
  - `../qa-outside-root-denied.txt` remained absent after the turn.
- Same-session follow-up read succeeded:
  - `cargo run --bin quine -- run --json --socket /tmp/quine-047.sock --session f69a27c9-f8b5-4656-849a-cdbd7801a239 "Now use read_file on Cargo.toml and tell me the workspace package names you see."`
  - `tool_calls` showed `read_file`.
  - The response listed `quine-core`, `quine-llm`, `quine-harness`, `quine-cli`, and `quine-sdk`.
- One-off in-workspace search succeeded:
  - `cargo run --bin quine -- run --json --socket /tmp/quine-047.sock "Use find with path '.' pattern 'Cargo.toml' and type 'file'. Report the exact relative path you found."`
  - `tool_calls` showed `find`.
  - The response included `Cargo.toml` and the crate-relative manifest paths under `crates/`.
- Stopped the daemon with `cargo run --bin quine -- daemon stop --socket /tmp/quine-047.sock`.

## Notes

- The `find` daemon scenario also reported `.claude/worktrees/.../Cargo.toml` paths because those directories are inside the current workspace root. That behavior is consistent with the feature scope: `047` enforces boundary containment, not content-based path filtering within the workspace.
