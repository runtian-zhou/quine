# QA Report — Feature 048 Bash Command Risk Policy

**Status**: passed

## Scope

- `features/048-bash-command-risk-policy.md`
- `crates/quine-core/src/permission/command.rs`
- `crates/quine-core/src/permission/request.rs`
- `crates/quine-core/src/permission/engine.rs`
- `crates/quine-core/src/engine.rs`
- `crates/quine-core/src/tool/bash.rs`

## Targeted Coverage

- `cargo test -p quine-core bash::tests -- --nocapture`
- `cargo test -p quine-core permission -- --nocapture`

## Results

- Focused classifier and evaluator coverage passed.
- Workspace validation passed:
  - `cargo build`
  - `cargo test`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo fmt --all -- --check`

## Daemon Evidence

- Independent QA smoke on `/tmp/quine-048.sock` matched the intended policy split:
  - `pwd` completed without approval and returned `/Users/runtianz/quine`
  - `touch qa-048-risk.txt` emitted `interaction_needed: true`
  - the approval prompt reason was `permission resolved by Default mode default for mutating bash command`
  - `qa-048-risk.txt` was not created

## Notes

- `048` keeps shell execution semantics unchanged and only changes permission metadata plus evaluator behavior.
- The first-release policy is intentionally conservative: low-risk inspection commands are auto-allowed, while mutating, nested-shell, interpreter-launch, and unknown commands remain approval-gated.
