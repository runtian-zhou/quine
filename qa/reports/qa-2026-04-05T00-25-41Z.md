# QA Report — Feature 050 Headless and Background Permission Behavior

**Status**: passed

## Scope

Verified deterministic non-interactive permission handling for headless one-shot sessions and background scheduler sessions, plus the interactive approval path remained intact after the prompt-behavior plumbing changes.

## Automated Evidence

- `cargo test -p quine-core permission:: -- --nocapture`
  - passed
- `cargo test -p quine-harness scheduler -- --nocapture`
  - passed
- `cargo test -p quine-core user_message_produces_turn_complete -- --nocapture`
  - passed
- `cargo test -p quine-core bash_tool_emits_permission_prompt_before_completion -- --nocapture`
  - passed
- `cargo test -p quine-harness local_harness_routes_permission_approval_and_resumes_turn -- --nocapture`
  - passed

## Daemon QA

- Daemon start:
  - `cargo run --bin quine -- daemon start --socket /tmp/quine-feature-050.sock`
- Headless one-shot run:
  - `cargo run --bin quine -- run --json --socket /tmp/quine-feature-050.sock "Use bash to run 'touch qa-050-headless.txt'. If permission is denied because no prompt is available, report that denial explicitly and stop."`
  - returned session `db55b936-45d1-4878-8c9f-3f97c7aeb27d`
  - assistant response explicitly reported denial because the session was headless and could not satisfy the approval prompt
- File-system safety check:
  - `test ! -e qa-050-headless.txt && echo absent || echo present`
  - result: `absent`
- Session state check after completion:
  - `cargo run --bin quine -- ps --json --socket /tmp/quine-feature-050.sock`
  - session `db55b936-45d1-4878-8c9f-3f97c7aeb27d` reported `status: "idle"`
- Daemon cleanup:
  - `cargo run --bin quine -- daemon stop --socket /tmp/quine-feature-050.sock`
  - follow-up cleanup returned `No such file or directory (os error 2)`, confirming the socket was already gone

## Regression Found and Fixed During QA

- The first headless daemon run exposed a stale session-state issue where a completed one-shot session remained listed as `streaming`.
- Fix landed in `crates/quine-core/src/engine.rs` by emitting `SessionStateChanged { state: Idle }` on the relevant successful and error completion paths.
- Re-run daemon QA confirmed the completed headless session now reports `idle`.

## Full Workspace Gates

- `cargo build`
  - passed
- `cargo test`
  - passed
- `cargo clippy --all-targets -- -D warnings`
  - passed
- `cargo fmt --all -- --check`
  - passed
