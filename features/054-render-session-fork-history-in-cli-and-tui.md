---
status: done
---

# Render Session Fork History in CLI and TUI

Expose session ancestry metadata through the harness and render it in CLI and TUI inspection surfaces so operators can understand parent-child fork relationships directly from the session UI.
