[package]
name = "quine"
version = "0.1.0"
edition = "2021"
description = "A sample CLI tool with --json output support"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
