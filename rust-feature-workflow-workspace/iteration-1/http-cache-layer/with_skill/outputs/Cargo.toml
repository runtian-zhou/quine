[package]
name = "http-cache"
version = "0.1.0"
edition = "2021"
description = "An HTTP client with a configurable in-memory caching layer for GET requests."
license = "MIT OR Apache-2.0"

[dependencies]
# No external dependencies for the initial version.
# Future iterations might add:
# lru = "0.12"      -- for LRU eviction
# reqwest = "0.12"   -- for a real HTTP backend

[dev-dependencies]
# mockito = "1"  -- for integration tests with a mock HTTP server
